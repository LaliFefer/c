namespace DO;

public record Product(
    int IDNumber,
    string ProductName,
    Categories Category,
    double Price,
    int QuantityInStock 
);

// הסבר: מבנה נתונים (record) זה מייצג מוצר ב-DO.
// שדות: מזהה, שם, קטגוריה (enum), מחיר וכמות במלאי.
