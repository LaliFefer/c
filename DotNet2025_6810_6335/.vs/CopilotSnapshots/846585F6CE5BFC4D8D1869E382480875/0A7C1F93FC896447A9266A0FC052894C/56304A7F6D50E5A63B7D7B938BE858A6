namespace DalApi;

public interface IDal
{
    // תכונה עבור ממשק המוצרים
    IProduct Product { get; }

    // תכונה עבור ממשק הלקוחות
    ICustomer Customer { get; }

    // תכונה עבור ממשק המכירות
    ISale Sale { get; }

    // הערה: IDal הוא הממשק שמאגד את כל תתי-המשקים של שכבת ה-DAL.
    // כך בשכבה העליונה נשתמש בשדה מסוג IDal אחד ולא בכמה שדות נפרדים.
}